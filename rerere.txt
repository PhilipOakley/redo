#include <stdio.h>

int main()
{
  printf("Hello feature branch 1\n");
  
  /*
   * This is a sample program that has two changed hunks
   * with changes on each side branch, which are not
   * automerged, but instaead manually resolved and recorded
   * in the rerere database.
   */
  
  printf("Git redo/rerere feature branch 1\n");
  
  return 0;
}